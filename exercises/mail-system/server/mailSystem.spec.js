import mailSystem from './mailSystem'

test('should handle...', () => {
  // arrange
  const to = '...'
  const subject = '...'
  const model = '...'

  // act
  mailSystem.sendWelcomeMail(to, subject, model)

  // assert
})
